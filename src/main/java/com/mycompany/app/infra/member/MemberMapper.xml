<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.app.infra.member.MemberMapper">

    <resultMap id="resultMapObj" type="com.mycompany.app.infra.member.Member"></resultMap>
   
    <select id="selectList" resultMap="resultMapObj">
    	SELECT *
    	FROM
    		member
    	WHERE 1=1
    	AND
    	 name LIKE CONCAT('%',#{shKeyword},'%')
    		
    
    </select>
    
	<select id="selectOne" resultMap="resultMapObj">
		SELECT *
		FROM 
			member
		WHERE 1=1
		AND
			seq = #{seq}
	</select>
	
	<update id="update">
        UPDATE     member
        SET     id = #{id},
       			password = #{password},
       			gender = #{gender},
       			tel = #{tel},
       			tel_type = #{tel_type},
				delYN=#{delYN}        	
        WHERE  1=1
        AND
           seq = #{seq}  
    </update>
    
    <delete id="delete">
    	DELETE 
    	FROM member
    	WHERE 1=1
    	AND 
    	seq = #{seq} 
    </delete>
    
    <insert id="insert"> 
    INSERT
    INTO member
    (id,password,gender,tel,tel_type,
    delYN)
    VALUE (#{id},#{password},#{gender},#{tel},#{tel_type},#{delYN})
    </insert>
    
    <update id="uelete">
        UPDATE 
        	member
        SET 
        	delYN = #{delYN}
        WHERE 1=1
        AND
        	seq = #{seq}
    </update>
    
   
    
   



	
</mapper>   

